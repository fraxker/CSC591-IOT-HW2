version: "3.9"
services:
  server:
    build: 
      dockerfile: Dockerfile.server
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
  client:
    depends_on:
      - server
    build: 
      dockerfile: Dockerfile.client
    volumes:
      - ../DataFiles:/DataFiles
